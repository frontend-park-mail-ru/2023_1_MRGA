import {Header} from "components/App/header/header";
import {FormContainer} from "components/UI/containers/formContainer/formContainer";

export const AboutPage = () => {
    return (
        <>
            <Header></Header>
            <FormContainer>
                <div>Всем привет. Это About Page</div>
            </FormContainer>

        </>
    )
}